{
  "_comment": "Builder Loops verification manifest. test = the episode-setup smoke suite; acceptance = the runnable check covering the new-episode setup flow (a hard gate: a PR that breaks these is auto-closed).",
  "test": "node scripts/run-tests.mjs",
  "acceptance": "node scripts/run-tests.mjs"
}
