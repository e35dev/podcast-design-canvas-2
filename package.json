{
  "name": "podcast-design-canvas-2",
  "version": "0.0.0",
  "private": true,
  "description": "Self-serve visual podcast production workspace — episode setup flow.",
  "scripts": {
    "preview": "python3 -m http.server 8080",
    "test": "node scripts/run-tests.mjs",
    "preview-build": "node scripts/preview-build.mjs"
  }
}
